import React, { FunctionComponent } from "react";
import styles from "./BitterLogo.module.scss";

const BitterLogo: FunctionComponent = (props) => {
  return <div className={styles.bitterLogoContainer}>bitter</div>;
};

export default BitterLogo;
